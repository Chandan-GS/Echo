import 'package:flutter/material.dart';

class AiModeScreen extends StatelessWidget {
  const AiModeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('AI Mode Selector Screen')));
  }
}
