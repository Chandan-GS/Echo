import 'package:flutter/material.dart';

class MiniBriefingScreen extends StatelessWidget {
  const MiniBriefingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Error: Mini-Briefing Screen'),
      ),
    );
  }
}
